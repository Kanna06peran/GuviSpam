
{
  "name": "voiceshield-ai-portal",
  "version": "1.1.0",
  "private": true,
  "description": "Production-ready VoiceShield AI Detection API Portal and Sandbox",
  "scripts": {
    "start": "npx serve .",
    "build": "echo 'Static ESM deployment - no build required' && exit 0"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
